# File ini harus berada di ROOT folder (sama level dengan folder public/)
# Tujuannya adalah redirect semua request ke folder public/

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Jika hosting Anda sudah point domain ke folder public/, file ini tidak diperlukan

